import { Canvas, Source } from '@storybook/addon-docs/blocks';

import * as TextAlignStories from './TextAlign.stories';

# Text Align

Adjust text alignment with the following utility classes. Text is aligned left by default.

To adjust text alignment at a specified breakpoint, use the format `-text-{direction}-{breakpoint}`.

`{direction}` can be:

-   `left`
-   `center`
-   `right`

`{breakpoint}` can be:

-   `xs`
-   `sm`
-   `md`
-   `lg`
-   `xl`

Breakpoint-specific classes cascade downwards.

## Component Example

<Canvas of={TextAlignStories.Default} />

## Source

<Source of={TextAlignStories.Default} />
