import { Canvas, Source } from '@storybook/addon-docs/blocks';

import * as ZIndexStories from './ZIndex.stories';

# Z-Index

Adjust any element's z-index. Higher values render in front of lower values, -z-neg will render z-index: -1.

The available classes are:

-   `-z-1` through `-z-9`
-   `-z-neg`

## Component Example

<Canvas of={ZIndexStories.Default} />

## Source

<Source of={ZIndexStories.Default} />
