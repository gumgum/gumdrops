import { Canvas, Source } from '@storybook/addon-docs/blocks';

import * as PointerEventsStories from './PointerEvents.stories';

# Pointer Events

Enable or disable the default user interaction with any element.

The available classes are:

-   `-pointer-events--auto`
-   `-pointer-events--none`

## Component Example

<Canvas of={PointerEventsStories.Default} />

## Source

<Source of={PointerEventsStories.Default} />
