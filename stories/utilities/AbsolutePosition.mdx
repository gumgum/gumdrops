import { Canvas, Source } from '@storybook/addon-docs/blocks';

import * as AbsolutePositionStories from './AbsolutePosition.stories';

# Absolute Position

Absolutely position elements in a corner of their parent element.

**Note**: This requires the parent element to have a non-static position (such as relative or absolute).

The available classes are:

-   `-pos-abs` Position Absolute
-   `-abs-t-r` Absolute Top Right
-   `-abs-t-l` Absolute Top Left
-   `-abs-b-r` Absolute Bottom Right
-   `-abs-b-l` Absolute Bottom Left

-   `-pos-rel` Position Relative
-   `-pos-fix` Position Fixed
-   `-pos-stc` Position Static

## Component Example

<Canvas of={AbsolutePositionStories.Default} />

## Source

<Source of={AbsolutePositionStories.Default} />
