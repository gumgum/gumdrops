import { Meta } from '@storybook/addon-docs/blocks';

<Meta title="Utilities Note" />

## Utilities

The design system offers utility classes that can be applied to any element. The stories in the `Utilities` section show the available classes and how they can be used.
