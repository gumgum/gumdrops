import { Story, Props } from '@storybook/addon-docs/blocks';
import ButtonGroup from '../../../components/atoms/ButtonGroup';

# ButtonGroup

The `<ButtonGroup>` wraps a series of `<Button>` components with a group prop.

## Component Example
<Story
    id="atoms-buttongroup--default"
/>

## Props
<Props of={ButtonGroup} />
