import { Story, Props } from '@storybook/addon-docs/blocks';
import FormGroupTextHelp from '../../../components/atoms/FormGroupTextHelp';

# FormGroupTextHelp

`<FormGroupTextHelp>` is used to give warning or hints on `<input>` elements. They must be wrapped by a `<FormGroup>` component.

The color of this component is dependent on the `FormGroup's context`.

## Component Example
<Story
    id="atoms-formgrouptexthelp--default"
/>

## Props
<Props of={FormGroupTextHelp} />
