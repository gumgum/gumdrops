import { Story, Props } from '@storybook/addon-docs/blocks';
import FormGroupLabel from '../../../components/atoms/FormGroupLabel';

# FormGroupLabel

The `<FormGroupLabel>` component is used to add a label to your `<FormGroup>`.

## Component Example
<Story
    id="atoms-formgrouplabel--default"
/>

## Props
<Props of={FormGroupLabel} />
