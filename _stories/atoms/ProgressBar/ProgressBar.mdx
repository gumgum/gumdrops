import { Story, Props } from '@storybook/addon-docs/blocks';
import ProgressBar from '../../../components/atoms/ProgressBar';

# ProgressBar
`<ProgressBar>` is a single element component. It accepts a data-value attribute with an integer value between 0 and 100, which is used to style the component and indicate elapsed progress of an event.

## Component Example
<Story
    id="atoms-progressbar--default"
/>

## Props
<Props of={ProgressBar} />
