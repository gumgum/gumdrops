import { Story, Props } from '@storybook/addon-docs/blocks';
import ImagePreview from '../../../components/molecules/ImagePreview';

# ImagePreview

The `<ImagePreview>` component is used to preview images or gifs.

It will pre-load the image and show a loading indicator until resolved, then the preview will display.


## ImagePreview example
```jsx
<ImagePreview src="PATH_TO_IMAGE">
  <Icon icon="plus" />
</ImagePreview>
```

## Changing the height of the preview
```jsx
<ImagePreview src="PATH_TO_IMAGE" maxHeight={100}>
  <Icon icon="plus" />
</ImagePreview>
```

## Component Example
<Story
    id="molecules-imagepreview--default"
/>

## Props
<Props of={ImagePreview} />
