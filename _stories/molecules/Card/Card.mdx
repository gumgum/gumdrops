import { Story, Props } from '@storybook/addon-docs/blocks';
import Card from '../../../components/molecules/Card';

# Card
The `<Card>` component is a standard container component, designed to hold groups of related information. Cards are usually arranged in a uniform grid.

You can nest `<CardBlock>` and `<CardImage>` components within a `<Card>`.

## Component Example
<Story
    id="molecules-card--default"
/>

## Props
<Props of={Card} />
