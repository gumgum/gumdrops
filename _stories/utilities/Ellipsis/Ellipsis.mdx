import { Story } from '@storybook/addon-docs/blocks';

# Ellipsis

Truncate text with the `-ellipsis` utility class. To apply the effect at specific breakpoints only, use the breakpoint-specific modifier classes.

The breakpoint-specific modifier classes that can be added to `-ellipsis` are:
* `--sm`
* `--md`
* `--lg`
* `--xl`

For example: `-ellipsis--sm`

## Component Example
<Story
    id="utilities-ellipsis--default"
/>
