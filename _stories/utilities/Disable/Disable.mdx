import { Story } from '@storybook/addon-docs/blocks';

# Disable

Add the helper class `-disabled` to any element to indicate it is disabled.

**Note**: This only affects the styles. You should add a `disabled` prop to a component to disable the user's interaction as well.

## Component Example
<Story
    id="utilities-disable--default"
/>
